{
  "id": "openclaw-smart-agent",
  "name": "OpenClaw Smart Agent",
  "description": "Expose Smart Agent runtime operations as OpenClaw tools.",
  "version": "0.1.0",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "autoStartRuntime": {
        "type": "boolean",
        "default": true,
        "description": "Auto-start the Smart Agent runtime when OpenClaw Gateway loads this plugin."
      },
      "runtimeConfigPath": {
        "type": "string",
        "minLength": 1,
        "description": "Optional config file path for the auto-started Smart Agent runtime."
      }
    }
  }
}
